package com.pvc.game.comman.exception;

public enum ErrorCode {

    USER_NOT_FOUND,
    INVALID_CREDENTIALS,
    TOKEN_EXPIRED,
    TOKEN_REVOKED,
    ACCESS_DENIED,
    VALIDATION_ERROR,
    RESOURCE_NOT_FOUND,
    INTERNAL_ERROR
}
